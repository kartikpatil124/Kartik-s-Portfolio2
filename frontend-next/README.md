This directory contains the production Next.js app for Kartik Patil's portfolio.

## Development

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
npm run lint
```

The app is configured with `output: "export"` so the build is emitted to `out/` for static hosting on Netlify.
